/*
        tableview (mobsModel.mobsList){
            title = "Mobs"
            column("Урон", Mob::damageProperty).contentWidth(padding = 50.0)
            column("Стоимость", Mob::costProperty).contentWidth(padding = 50.0)
            column("Здоровье", Mob::healthProperty).contentWidth(padding = 50.0)
            column("Скорость", Mob::speedProperty).contentWidth(padding = 50.0)
            column("Тип", Mob::typeProperty).contentWidth(padding = 50.0)
            column("Атаковать", Mob::canAttack).contentWidth(padding = 50.0)
            column("Область атаки", Mob::attackRange).contentWidth(padding = 50.0)
            //column("Количество", modeModel.waves.).contentWidth(padding = 50.0)
            //column("Количество", MobWithCount::countProperty).contentWidth(padding = 50.0).makeEditable()
            bindSelected(mobModel)
            columnResizePolicy = SmartResize.POLICY
        } */




TornadoFX is a lightweight JavaFX framework for Kotlin that offers several advantages over using JavaFX alone. Some of the key benefits include:
Simplified code: TornadoFX minimizes the amount of code needed to build JavaFX applications, making it easier to create and maintain user interfaces
2
.
Type-safe builders: It includes type-safe builders to quickly lay out controls and user interfaces, ensuring that your code is both maintainable and evolvable
2
.
Dependency injection: TornadoFX provides dependency injection, allowing for better organization and flexibility in your application's architecture
5
.
Delegated properties and control extension functions: These features, enabled by Kotlin, help simplify codebases and tackle the verbosity of UI code
2
.
Compatibility with JavaFX libraries: TornadoFX can work in conjunction with other popular JavaFX libraries such as ControlsFX and JFXtras, allowing you to leverage existing solutions
2
.
Kotlin interoperability: TornadoFX takes advantage of Kotlin's 100% interoperability with existing Java libraries and codebases, including JavaFX
2
.
Open-source and independent of VM release cycle: TornadoFX is an open-source project and is not dependent on the VM release cycle, providing more flexibility in its development and updates
3
.



        //val wavesList =
        //val wavesList = mobs.map { MobWithCount(it, countOfWaves.value) }.toCollection(ArrayList())
        //waves.setAll(wavesList)





        tableview(modeModel.wavesLists[modeModel.selectedWave.value]) {
            title = "Mobs"
            column("Моб", MobWithCount::mobProperty).contentWidth(padding = 50.0)
            column("Количество", MobWithCount::countProperty).contentWidth(padding = 50.0).makeEditable()
            columnResizePolicy = SmartResize.POLICY
        }